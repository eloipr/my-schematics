{
  "$schema": "http://json-schema.org/schema",
  "$id": "PrettierSchematics",
  "title": "Prettier Options Schema",
  "type": "object",
  "description": "Creates prettier configuration",
  "properties": {
    "printWidth": {
      "enum": [80, 100, 120, 140],
      "default": 120,
      "description": "Specify the line length that the printer will wrap on.",
      "x-prompt": "Specify the line length that the printer will wrap on (default: 120)"
    },
    "tabWidth": {
      "enum": [2, 4],
      "default": 2,
      "description": "Tab width.",
      "x-prompt": "What tab width do you want? (default: 2)"
    },
    "singleQuote": {
      "type": "boolean",
      "default": true,
      "description": "Use single quotes.",
      "x-prompt": "Do you want to use single quotes? (default: true)"
    }
  }
}
